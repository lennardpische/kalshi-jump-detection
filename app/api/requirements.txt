# Inference service (CPU-only; the gate is a tiny MLP)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
numpy>=1.24
# CPU build keeps the image small enough for free tiers:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.1

# Phase 2 (real experts from raw trades) will additionally need:
# lightgbm, pandas, pyarrow, and whatever the Mamba/Moirai/CTTS/FTT models require.
